"""I/O adapters for the fake OpenCode CLI surface: state-file, HTTP server, HTTP
client, and subprocess plumbing. Package-private — ``cli.py`` is the only caller.
"""

from __future__ import annotations
