"""Hub-store concept composers (``bzh:screaming-architecture``).

Every composer here lands rows into the **hub** store — including
``runner_pause_seed.py``, whose ``runner_`` prefix names the hub-side fleet
concept, not the runner store.
"""
